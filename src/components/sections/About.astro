---
import { getLangFromUrl, useCV, useHeadersTranslations } from "@/utils";
import Section from "../Section.astro";

const lang = getLangFromUrl(Astro.url);
const headers = useHeadersTranslations(lang);
const {
  basics: { summary },
} = await useCV(lang);
---

<Section title={headers.about}>
  <p>
    {summary}
  </p>
</Section>
